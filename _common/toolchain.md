# Toolchain Experience

> CLI, IDE, CI/CD, deployment notes, and dev environment configurations.
> AI will match keywords from `_catalog.json` to load this file when relevant.

<!-- Example entry:
## pnpm Workspace Setup
**Date:** 2025-01-10
**Context:** Monorepo with shared packages
**Best Practice:**
- Use `pnpm-workspace.yaml` at root
- Set `shamefully-hoist=true` only if legacy deps require it
- Prefer `catalog:` protocol for shared dependency versions
**Keywords:** pnpm, monorepo, workspace, package-manager
-->
